# Local dev: run `npm run db:dev` (Prisma local Postgres) and paste the URL it prints.
# Production: a Neon/any Postgres connection string.
DATABASE_URL="postgres://user:password@host/db"

# Generate with: openssl rand -base64 32
AUTH_SECRET="change-me"
